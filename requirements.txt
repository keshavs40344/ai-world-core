# ============================================================
# Project Genesis — Python Dependencies
# ============================================================

# --- Local LLM ---
ollama>=0.3.0

# --- Vector Store ---
chromadb>=0.5.0
sentence-transformers>=3.0.0

# --- Docker Sandboxing ---
docker>=7.0.0

# --- Scheduling ---
apscheduler==3.11.3          # 4.0 is still alpha — do NOT upgrade
pysqlite3-binary>=0.5.0       # ChromaDB SQLite fallback (if system SQLite < 3.35)

# --- HTTP / Scraping ---
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# --- Git ---
gitpython>=3.1.40

# --- Database ---
# sqlite3 is stdlib — no pip install needed

# --- CLI & Display ---
rich>=13.7.0
click>=8.1.7

# --- System Metrics ---
psutil>=5.9.0

# --- Code Quality (used inside Docker workers too) ---
ruff>=0.4.0
pytest>=8.0.0
pytest-cov>=5.0.0

# --- Data ---
pydantic>=2.7.0

# --- Dashboard (Phase 7) ---
streamlit>=1.35.0

# --- Utilities ---
python-dotenv>=1.0.0
tenacity>=8.3.0

# --- Web Server / API ---
fastapi
uvicorn
