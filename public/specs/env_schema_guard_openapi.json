{
  "openapi": "3.0.0",
  "info": {
    "title": "env_schema_guard",
    "version": "1.0.0",
    "description": "Prevents runtime crashes in microservices by validating environment variables against a strict schema before application boot, catching typos and missing defaults immediately."
  },
  "paths": {
    "/process": {
      "post": {
        "responses": {
          "200": {
            "description": "OK"
          }
        }
      }
    }
  }
}